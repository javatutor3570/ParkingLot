package org.vehicle.parking.service;

import org.vehicle.parking.model.Spot;
import org.vehicle.parking.model.Vehicle;

public class ParkingLotImpl implements ParkingLot {
     private Spot getSpot(Vehicle v){
         // Determine the size of vehicle necessar to reserve spot getSize(v);
         // getSpot(SpotSize s);
         // is spot available yes place on that spot else say parking unavailable
         return null;
     }

    @Override
    public Spot placeVehicle(Vehicle v) {
        return null;
    }

    @Override
    public Spot removeVehicle(Vehicle v) {
        return null;
    }
}
